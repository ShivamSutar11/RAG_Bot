# RAG_Bot
A lightweight, document-restricted RAG chatbot built entirely for local use. It uses Qwen 0.5B for generation, MiniLM for fast embeddings, and FAISS for efficient similarity search — making the whole pipeline smooth even on a Mac.
